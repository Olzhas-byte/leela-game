/**
 * Конфигурация правил игры. Все «спорные» правила (различаются между изданиями
 * «Лилы») вынесены сюда, чтобы менять их в одном месте, не трогая логику.
 */
export const GAME_CONFIG = {
  /** Для входа в игру нужно выбросить 6. */
  ENTRY_REQUIRES_SIX: true,

  /** Клетка, на которую игрок попадает при «рождении» (входе) выпавшей 6. */
  ENTRY_SQUARE: 6,

  /** Выпавшая 6 даёт право на дополнительный бросок. */
  EXTRA_TURN_ON_SIX: true,

  /** Клетка-цель: на неё нужно встать ТОЧНО, чтобы завершить игру. */
  WIN_SQUARE: 68,

  /** Максимальный номер клетки на доске. */
  BOARD_MAX: 72,

  /**
   * Поведение при «перелёте» за край доски (newPos > BOARD_MAX):
   *  - 'stay'   — игрок остаётся на месте (ход «не вмещается»);
   *  - 'bounce' — отражение: лишние шаги отсчитываются назад от края.
   * В классической «Лиле» перелёт мимо цели «ловится» змеёй 72→51,
   * поэтому за саму доску выходить нельзя — оставляем 'stay'.
   */
  OVERSHOOT_BEHAVIOR: 'stay' as 'stay' | 'bounce',

  /** Кубик: грани от 1 до DICE_MAX. */
  DICE_MAX: 6,
  DICE_MIN: 1,
} as const;

export type GameConfig = typeof GAME_CONFIG;
